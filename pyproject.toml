[project]
name = "end2end-recommendation-system"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Nicholas Ward", email = "nickhward@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "datasets>=4.8.5",
]

[project.scripts]
end2end-recommendation-system = "end2end_recommendation_system:main"

[build-system]
requires = ["uv_build>=0.11.16,<0.12.0"]
build-backend = "uv_build"
